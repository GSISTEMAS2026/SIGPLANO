package GS_SEDUC.SIGPLANO_BACKEND.model.enums;

/**
 * Roles de conta de setor do SIGPLANO. Toda conta de setor (Usuario) pertence
 * a exatamente uma destas roles; não são mais administráveis via tabela.
 */
public enum Role {
    SUPERINTENDENCIA,
    PLANEJAMENTO,
    ORCAMENTO,
    ADMIN
}
